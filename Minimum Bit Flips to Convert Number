class Solution {
    public int minBitFlips(int start, int goal) {
        if(start==0 && goal==0)
        return 0;
       int ans=start^goal;
       int c=0;
       while(ans>=1)
       {
        c+=(ans&1);
        ans>>=1;
       } 
       if(ans==1)
       c++;
       return c;
    }
}
